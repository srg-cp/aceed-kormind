# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Build
[Bb]in/
[Oo]bj/
# Salida de precompilación de aspnet_compiler (artefacto, no se versiona)
_program files_git/

# NuGet (estilo packages.config: los paquetes no se versionan)
packages/
*.nupkg
nuget.exe

# Secretos (claves de Gemini / Google OAuth) — usar Secrets.config.example como plantilla
Secrets.config
.claude/
.claude

# Datos locales de la aplicación
grpSPEF/pjtSPEF/App_Data/storage/
# Almacén local de auth (usuarios + refresh token cifrado): es runtime, no se versiona
grpSPEF/pjtSPEF/App_Data/*.json
*.mdf
*.ldf

# Otros
*.log
